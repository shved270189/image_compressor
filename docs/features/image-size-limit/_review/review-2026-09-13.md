---
status: CHANGES REQUESTED
feature: image-size-limit
date: "2026-09-13"
feature_size: S
route: quick
---

# Review — image-size-limit

## Scope

- Size S + route quick (from `.size` / `.route`).
- Branch `feature/image-size-limit`. Merge-base `af40862` (`master`).
- Diff: `af40862..HEAD`. Stat: 14 files, +1102 / −57. Working tree at review start also had unstaged `sad.md` / `roadmap.md` and untracked SDD artifacts (`spec.md`, `ux-flows.md`, `contracts/`, `tasks.json`, task files, `CONTEXT.md`). Review traced on-disk artifacts, not only the committed trailers.
- Implementation commits carry `SDD-AC` trailers for AC-01..AC-12. Review traced the whole spec §4 user-story set and §5 AC set (AC-01..AC-12), not a trailer subset.
- Surfaces: `backend-service`, `web-frontend`.
- Reviewers: one clean-context reviewer pass (stage 1 AC/trace/contracts + stage 2 quality). Judgment model degraded to session inherit (`grok-4.6`; configured `opus` unavailable). `sdd:reviewer` unavailable; fallback `general-purpose` with the reviewer prompt.
- `data-model.md` absent — N/A (no schema). `docs/design-system.md` absent — screens native fallback, no `NEW` components.

## Stage 1

Trace of US-01..US-05 and AC-01..AC-12: every §4 user story has ≥1 AC and a §6 flow; `target_surfaces` both reached; ADRs 0001–0002 match live `POST /api/v1/images/process` headers. AC-10 code exists but has no e2e-through-UI test (F1).

## Findings

### F1 — AC-10 has no e2e-through-UI test

- **Stage:** 1
- **Cite:** `frontend/src/App.tsx:61`; `frontend/src/App.tsx:149`; AC-10; spec test plan AC-10 rows; T5; `_audit/browser-acceptance.md`
- **Problem:** New-file reset of Preview, miss facts, errors, maxima, format and Size limit is implemented in `selectFile` (`setMiss(null)`, empty bound, `sizeUnit` `mb`, JPEG). Stale completions are skipped at `App.tsx:149`. Spec test plan requires e2e-through-UI for both branches. T7 and `_audit/browser-acceptance.md` never record new-file-after-miss or delayed-work suppression (`test_size_limit_miss_and_submit_wiring` only greps source).
- **Suggested:** Add a browser-acceptance row (or extend the existing smoke/e2e path) that after a miss selects another file and asserts miss copy, bound, unit Mb, JPEG and preview from the old file are gone, and that an old completion cannot download or restore miss facts.
- **Verdict:** Fix now (owner, 2026-09-13).

### F2 — Size limit help copy from screens.md never landed

- **Stage:** 2
- **Cite:** `frontend/src/App.tsx:212`; `screens.md:163`; `screens.md:193`; AC-02 (layout, not AC text)
- **Problem:** WF-01/WF-02 require `Leave empty for no result size bound.` The control has label, radios and `placeholder="None"` only.
- **Suggested:** Add that help text under the Size limit row, `aria-describedby` on `#size-limit`.
- **Verdict:** Fix now (owner, 2026-09-13).

### F3 — architecture-map still states extra-shrink is forbidden

- **Stage:** 2
- **Cite:** `docs/architecture-map.md:111`; T8; `README.md:13`; `docs/roadmap.md:44`
- **Problem:** T8 marks size-limit as materialized, but the same map section still says image-resize-convert AC-05 forbids extra shrink and D2 must be closed, contradicting this spec and README.
- **Suggested:** Replace that sentence with the closed D2 rule (bound may undercut maxima; encoder-limit 422 unchanged).
- **Verdict:** Fix now (owner, 2026-09-13).

### F4 — T7 did not observe the loading lock it claims for AC-11

- **Stage:** 2
- **Cite:** `_audit/browser-acceptance.md:20`; `frontend/src/App.tsx:201`; `frontend/src/App.tsx:205`; AC-11
- **Problem:** Lock is `fieldset disabled={!file || busy}` plus `disabled={busy}` on the file input. The audit states the miss encode finished before a mid-flight snapshot. Recoverable-failure restore and stale-completion ignore are also unnamed in the table.
- **Suggested:** One mid-flight check that Size limit radios/number are disabled, plus a failure-retry row if T7 is re-run.
- **Verdict:** Fix now (owner, 2026-09-13).

## Follow-up for implement

Stay in context. Re-enter TDD for the four Fix-now items; do not re-open unrelated tasks.

1. **AC-10 e2e-through-UI.** After a miss, select another file. Assert miss copy, bound, unit Mb, JPEG and the old preview are gone. Assert a stale completion cannot download or restore miss facts. Record both branches in `_audit/browser-acceptance.md` as named in the spec test plan.
2. **Size limit help copy.** Add `Leave empty for no result size bound.` under the Size limit row and point `#size-limit` at it with `aria-describedby`. Keep 360 CSS px with no page overflow.
3. **architecture-map D2.** Replace the extra-shrink-forbidden / D2-open sentence so the map matches closed D2: a supplied bound may undercut maxima; encoder-limit 422 is unchanged.
4. **AC-11 live lock + failure retry.** During busy, record that Size limit radios and number are disabled. Add a recoverable-failure row that keeps the selected file and parameters.

## Gate

**CHANGES REQUESTED**

No unresolved finding remains without an owner verdict. All four findings are Fix now; they block `/sdd:ship` until re-reviewed on the changed surface.
