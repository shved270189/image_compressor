---
status: CHANGES REQUESTED
feature: image-resize-convert
date: "2026-09-06"
feature_size: M
route: standard
---

# Review — image-resize-convert

## Scope

- Size M + route standard (from `.size` / `.route`).
- Branch `survey/foundation` (only branch; no separate feature branch).
- Diff: `f060560..HEAD` (`f060560` test-plan → `40c8817` T13 complete).
- Stat: 34 files, +2700 / −55, 23 commits. Working tree was clean at review start.
- Commits carry no `SDD-AC` trailers. Review traced the whole spec §5 set (AC-01..AC-16), not a trailer subset.
- Surfaces: `web-frontend`, `backend-service`.
- Reviewers: two clean-context `sdd:reviewer` passes (stage 1 AC/trace/contracts; stage 2 quality). Judgment model degraded to session inherit (`grok-4.6`; configured `opus` unavailable).

## Stage 2

`REVIEW_CLEAN`: conventions, UI reuse, error/edge, security, boundaries, and test adequacy.

## Findings

### F1 — HEIC notice omits the no-HDR-appearance rule

- **Stage:** 1
- **Cite:** `frontend/src/App.tsx:189`; AC-10; `screens.md:88-90`; `test-plan.md:53`
- **Problem:** AC-10 requires the form to explain before submission that extra images are omitted, HDR becomes ordinary 8-bit output, **and** that there is no promise of retaining original HDR appearance. Live copy is `HEIC: only the primary image is kept. Extra images are omitted; HDR becomes ordinary 8-bit output.` The screens.md line is `HEIC: only the primary image is used; extra images are omitted. HDR becomes ordinary 8-bit output and may not retain its original appearance.` Recorded UI evidence (`_audit/implementation-selection.md:6`, `_audit/browser-acceptance.md:7-18`) never asserts the notice. Backend primary/HDR tests do not cover this UI clause.
- **Suggested:** restore the screens.md appearance disclaimer on the always-visible HEIC notice and record the e2e-through-UI check named in `test-plan.md:53`.
- **Verdict:** Fix now (owner, 2026-09-06).

### F2 — Preview orientation never proven on the UI surface

- **Stage:** 1
- **Cite:** `frontend/src/App.tsx:171`; AC-02, AC-09; `test-plan.md:34`, `test-plan.md:50`
- **Problem:** AC-02/AC-09 require any available Preview to show the complete, correctly oriented original, and that Preview and download agree. Preview is a native `img` object URL with no orientation fixture. T9 claims AC-09; T11 claims AC-02. Recorded browser evidence covers ordinary PNG preview / HEIC omission only. Orientation assertions exist only on the backend (`tests/test_images.py:133`, `tests/test_images.py:389`). A UI AC cannot close on that.
- **Suggested:** drive SCR-01 with a rotation/mirror fixture, assert Preview vs decoded download orientation (and oriented bounds), and record it in the T11 matrix.
- **Verdict:** Fix now (owner, 2026-09-06).

## Follow-up for implement

Stay in context. Re-enter TDD for the two Fix-now items; do not re-open unrelated tasks.

1. **AC-10 notice.** Set the HEIC help copy to the screens.md wording, including `and may not retain its original appearance.` Record the `test-plan.md:53` e2e-through-UI check (notice visible before upload, no content inspection).
2. **AC-02 / AC-09 preview orientation.** Use a JPEG or PNG rotation/mirror fixture the browser can display. Assert the Preview is uncropped and correctly oriented, that the decoded download matches, and that dimension limits apply to that orientation. Record the check in `_audit/browser-acceptance.md`.

## Gate

**CHANGES REQUESTED**

No unresolved stage-1 finding remains without an owner verdict. Both findings are Fix now; they block `/sdd:ship` until re-reviewed on the changed surface.
