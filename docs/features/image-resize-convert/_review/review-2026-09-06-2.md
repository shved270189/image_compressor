---
status: PASS
feature: image-resize-convert
date: "2026-09-06"
feature_size: M
route: standard
---

# Review — image-resize-convert (re-review)

## Scope

- Size M + route standard (from `.size` / `.route`).
- Branch `survey/foundation` (only branch; no separate feature branch).
- Re-review after `_review/review-2026-09-06.md` (`CHANGES REQUESTED`) and follow-up `4918067` (`fix(image-resize-convert): restore HEIC notice and pin preview orientation`; `SDD-AC: AC-02, AC-09, AC-10`).
- Diff: `f060560..HEAD` on feature surfaces (`backend`, `frontend`, `tests`, `docs/features/image-resize-convert`, `README.md`, `docs/architecture-map.md`, `pyproject.toml`). Ignore `9f95edc` (SDD install).
- Stat: 35 files, +2729 / −54. Working tree was clean at review start.
- Commits carry no `SDD-AC` trailers except `4918067`. Review traced the whole spec §5 set (AC-01..AC-16), not a trailer subset.
- Surfaces: `web-frontend`, `backend-service`.
- Reviewers: two clean-context reviewer passes (stage 1 AC/trace/contracts; stage 2 quality). Judgment model degraded to session inherit (`grok-4.6`; configured `opus` unavailable). `sdd:reviewer` unavailable; fallback `general-purpose` with the reviewer prompt.

## Previous findings

### F1 CLOSED — HEIC notice includes the appearance disclaimer

- **Cite:** `frontend/src/App.tsx:189`; `screens.md:88-90`; `tests/test_smoke.py:18`; `_audit/browser-acceptance.md:19`; `_audit/implementation-selection.md:8`; AC-10; `test-plan.md:53`
- Live copy matches screens.md: extra images omitted, HDR becomes ordinary 8-bit output, and may not retain its original appearance. Chrome e2e-through-UI recorded the notice on the empty form before any upload.

### F2 CLOSED — Preview orientation proven on the UI surface

- **Cite:** `tests/fixtures/images/oriented-exif6.jpg`; `_audit/browser-acceptance.md:20`; AC-02, AC-09; `test-plan.md:34`, `test-plan.md:50`
- Chrome Preview natural/display 8×12 vs width-4 download 4×6 JPEG; Pillow top cyan / bottom red; EXIF stripped. Bounds apply to the oriented size.

## Stage 1

`REVIEW_CLEAN`: AC-01..AC-16 traced spec → sequences §6 → data-model → openapi → tasks → implement (code + test / recorded e2e-through-UI). Every §4 user story has ≥1 AC and a §6 flow. UI ACs reach ui-layer tasks and browser-acceptance evidence.

## Stage 2

Conventions, UI reuse, error/edge, security, and boundaries: no new cited findings.

### F3 — HEIC-notice smoke pin is tautological

- **Stage:** 2
- **Cite:** `tests/test_smoke.py:18`; AC-10; `test-plan.md:53`; `Agents.md` smoke rule
- **Problem:** `test_heic_notice_copy` greps `frontend/src/App.tsx` and never asserts served `/assets` JS (host or `SMOKE_BASE_URL`).
- **Suggested:** pin `HEIC_NOTICE` in the JS already fetched by `test_skeleton`.
- **Verdict:** Not an issue (owner, 2026-09-06). AC-10 is closed by live copy plus the e2e-through-UI record. The source grep is a cheap copy pin, not the AC's test. A stale container would still fail other smoke asset/process checks; strengthening this pin is optional polish.

### F4 — Preview orientation fixture unused in pytest

- **Stage:** 2
- **Cite:** `frontend/src/App.tsx:171`; `tests/fixtures/images/oriented-exif6.jpg`; `_audit/browser-acceptance.md:20`; AC-02, AC-09
- **Problem:** no pytest opens `oriented-exif6.jpg`. Backend orientation still uses generated PNG EXIF. Playwright scripts lived in `/tmp/image-implementation-fixtures`.
- **Suggested:** process the JPEG in `tests/test_images.py` (oriented 8×12, cyan-top, width 4 → 4×6, EXIF stripped).
- **Verdict:** Not an issue (owner, 2026-09-06). The prior review asked for a SCR-01 rotation/mirror fixture and a recorded Preview vs download check; that is `_audit/browser-acceptance.md:20`. A backend pytest of the same file cannot close a UI AC (the prior review's own rule). Backend orientation coverage already exists.

## Gate

**PASS**

No unresolved stage-1 finding. Previous Fix-now items are closed. Both stage-2 findings dismissed with reason. Ready for `/sdd:ship image-resize-convert`.
