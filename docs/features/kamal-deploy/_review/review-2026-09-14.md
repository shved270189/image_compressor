---
status: PASS
feature: kamal-deploy
date: "2026-09-14"
feature_size: S
route: quick
---

# Review — kamal-deploy

## Scope

- Size S + route quick (from `.size` / `.route`).
- Branch `feat/kamal-deploy`. Merge-base `652c244` (`main`).
- Diff: `652c244..HEAD`. Stat: 8 files, +349 / −8.
  - `.gitignore`, `README.md`, `config/deploy.yml`, `docs/architecture-map.md`, `docs/features/kamal-deploy/tasks/tracker.md`, `tests/fixtures/kamal/secrets`, `tests/test_kamal_deploy.py`, `tests/test_smoke.py`
- Working tree at review start also had unstaged `sad.md` / `roadmap.md` and untracked SDD artifacts (`spec.md`, `CONTEXT.md`, `contracts/`, `tasks.json`, task files, `.size`, `.route`, `_diagrams/`). Review traced on-disk artifacts, not only the committed trailers.
- Implementation commits carry `SDD-AC` trailers for AC-01..AC-08 (T1–T8, all `done` in `tasks/tracker.md`). Review traced the whole spec §4 user-story set (US-01..US-06) and §5 AC set (AC-01..AC-08), not a trailer subset.
- Surfaces: `cli` only (`sad.md` `target_surfaces: [cli]`).
- Reviewers: one clean-context reviewer pass (stage 1 AC/trace/contracts + stage 2 quality). Judgment model degraded to session inherit (`grok-4.6`; configured `opus` unavailable). `sdd:reviewer` unavailable; fallback `general-purpose` with the reviewer prompt.
- `data-model.md` absent — N/A (`contracts/api-sync-report.md`). No `openapi.yaml`; CLI contract is `contracts/cli.md`. No `<!-- added-by-fix -->` ACs. No `ux-flows.md` / `screens.md` (no UI surface).

## Stage 1

Trace of US-01..US-06 and AC-01..AC-08: every §4 user story has ≥1 AC and a §6 flow; `target_surfaces: [cli]` reached via `contracts/cli.md` (`configurationCheck`, `laterPublish`); ADRs 0001–0002 match the live offline pytest check (no UI/backend surface, no Kamal network, no new CI job). Every AC reaches code + a test or the documented README contract.

| US / AC | Flow (`sad.md` §6) | Contract | Task | Code + test |
|---|---|---|---|---|
| US-01 / AC-01 | Record deploy configuration | `cli.md` recipe fields | T2, T4 | `config/deploy.yml:1-23`; `tests/test_kamal_deploy.py:180-200` |
| US-02 / AC-02 | Record — secrets stay local | `cli.md` `secret_in_committed_tree` | T1, T6 | `.gitignore:13`; `tests/test_kamal_deploy.py:238-261` |
| US-03 / AC-03 | Run configuration check | `configurationCheck` exit 0; no Kamal/network | T3, T4 | `tests/fixtures/kamal/secrets:1`; `tests/test_kamal_deploy.py:180-183` |
| US-04 / AC-04 | Run configuration check — missing branch | `configurationCheck` exit 1; glossary gaps | T5 | `tests/test_kamal_deploy.py:203-235` |
| US-05 / AC-05 | Record — README later command | `laterPublish` = `bundle exec kamal deploy` | T7 | `README.md:125-135` (docs contract; spec test plan type `contract`) |
| US-06 / AC-06 | Form has no publish action | form out of CLI surface | T8 | `tests/test_smoke.py:44-50` |
| US-06 / AC-07 | Form — process one image | existing process path unchanged | T8 | `tests/test_smoke.py:67-144`; `frontend/` / `backend/` untouched |
| US-01 / AC-08 | Record — port/path invariant | `production_target_invariant_violated` | T2, T4, T6 | `config/deploy.yml:13-15`; `tests/test_kamal_deploy.py:264-284` |

Evidence cited by the independent reviewer: `uv run pytest tests/test_kamal_deploy.py tests/test_smoke.py` → 12 passed; `uv run ruff check tests/test_kamal_deploy.py tests/test_smoke.py` → clean; `git check-ignore -q .kamal/secrets` → 0.

## Findings

`REVIEW_CLEAN: kamal-deploy records the locked recipe, gitignores secrets, proves the recipe offline, documents bundle exec kamal deploy, and leaves the form/smoke path unchanged.`

No cited findings. Nothing to resolve with Fix now / Defer / Not an issue.

## Gate

**PASS**

No unresolved stage-1 finding remains. Ready for `/sdd:ship kamal-deploy`.
